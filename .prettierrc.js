const config = {
  trailingComma: 'all', //virgula sobrando
  tabWidth: 2, // tamanho do TAB
  printWidth: 80, //largula da linha
  semi: true, //para ponto e virgula
  singleQuote: true, // aspas simples
};

module.exports = config;
